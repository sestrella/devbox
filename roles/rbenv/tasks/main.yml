---
- name: "Clone rbenv"
  git:
    repo: "{{ rbenv_repo }}"
    dest: "{{ rbenv_dir }}"
    version: "{{ rbenv_version }}"

- name: "Clone rbenv plugins"
  git:
    repo: "{{ item.value.repo }}"
    dest: "{{ rbenv_dir }}/plugins/{{ item.key }}"
    version: "{{ item.value.version }}"
  with_dict: "{{ rbenv_plugins }}"
