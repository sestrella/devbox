---
- name: Install package
  apt:
    name: zsh
    state: present
  become: true

- name: Change shell
  user:
    name: "{{ ansible_user_id }}"
    shell: /usr/bin/zsh
    state: present
  become: true
