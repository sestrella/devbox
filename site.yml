---
- hosts: localhost
  roles:
    # Dependencies
    - {role: fish, tags: fish}
    # General
    - {role: asdf, tags: asdf}
    - {role: autojump, tags: autojump}
    - {role: direnv}
    - {role: fzf}
    - {role: git, tags: git}
    - {role: homebrew, tags: homebrew}
    - {role: neovim, tags: neovim}
    - {role: starship, tags: starship}
    - {role: tmate, tags: tmate}
    - {role: tmux, tags: tmux}
